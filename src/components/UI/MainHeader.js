import React from 'react'

function MainHeader({header}) {
    return (
        <h1 className="text-blue-700 text-center text-4xl md:text-5xl font-bold">{header}</h1>
    )
}

export default MainHeader
